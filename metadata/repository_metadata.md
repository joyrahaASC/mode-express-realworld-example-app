# Repository Metadata Knowledge Base

## `src/app/models`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/app/models/http-exception.model.ts` | Defines a custom exception class for HTTP-related errors in the application. Extends the native Error class to include an HTTP error code alongside the error message, enabling structured error handling for HTTP responses. | <b>`constructor(errorCode: number, message: string | any)`</b>: Initializes a new HttpException instance with an HTTP error code and error message for propagating HTTP-specific errors through the application. |

## `src/app/routes`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/app/routes/routes.ts` | Serves as the central API routing configuration file for the Express RealWorld application. Aggregates and mounts all feature-specific controllers (tags, articles, profiles, authentication) under a unified '/api' prefix to establish the application's REST API structure. |  |

## `src/app/routes/article`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/app/routes/article/article.controller.ts` | Defines the Express router controller for article-related HTTP endpoints in a RealWorld API implementation. Orchestrates RESTful operations for articles, comments, and favorites by delegating to service layer functions and handling authentication middleware for protected routes. | <b>`router.get('/articles', auth.optional, async (req: Request, res: Response, next: NextFunction))`</b>: Retrieves paginated list of articles with optional filtering by tag, author, and favorited status. Authentication is optional to support both authenticated and anonymous users.<br/><b>`router.get('/articles/feed', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Returns paginated feed of articles from followed authors for authenticated users only.<br/><b>`router.post('/articles', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Creates a new article with title, description, body, and tag list for authenticated users.<br/><b>`router.get('/articles/:slug', auth.optional, async (req: Request, res: Response, next: NextFunction))`</b>: Fetches a single article by its slug identifier with optional authentication for personalized data.<br/><b>`router.put('/articles/:slug', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Updates an existing article's title, description, or body content for authenticated article owners.<br/><b>`router.delete('/articles/:slug', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Deletes an article by slug for authenticated article owners.<br/><b>`router.get('/articles/:slug/comments', auth.optional, async (req: Request, res: Response, next: NextFunction))`</b>: Retrieves all comments associated with a specific article by slug.<br/><b>`router.post('/articles/:slug/comments', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Adds a new comment to an article for authenticated users.<br/><b>`router.delete('/articles/:slug/comments/:id', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Deletes a specific comment by ID from an article for authenticated comment owners.<br/><b>`router.post('/articles/:slug/favorite', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Marks an article as favorited by the authenticated user.<br/><b>`router.delete('/articles/:slug/favorite', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Removes favorite status from an article for the authenticated user. |
| `src/app/routes/article/article.mapper.ts` | Transforms raw article data objects into a standardized article response format for the RealWorld API. Maps article properties including slug, title, description, body, tag list, timestamps, favorited status, favorites count, and author information using the author mapper. | <b>`const articleMapper = (article: any, id?: number)`</b>: Accepts an article object and optional user ID, returning a formatted article object with computed favorited status and mapped author details. |
| `src/app/routes/article/article.model.ts` | Defines the TypeScript interface for an Article entity in the RealWorld example application. This interface specifies the data structure for articles, including metadata fields like title, slug, description, and relationships to comments and favorited status. |  |
| `src/app/routes/article/article.service.ts` | Provides comprehensive service layer operations for article management in a RealWorld blogging application. Handles CRUD operations for articles, comments, and favorites, including querying with filters (author, tag, favorited), feed generation, and authorization checks. Integrates with Prisma ORM for database operations and applies business logic such as slug generation, validation, and user-specific data mapping. | <b>`export const getArticles = async (query: any, id?: number)`</b>: Retrieves a paginated list of articles based on query filters (author, tag, favorited) with support for demo mode and user-specific data. Returns articles with their metadata, tags, author profiles, and favorite counts.<br/><b>`export const getFeed = async (offset: number, limit: number, id: number)`</b>: Fetches a personalized feed of articles from authors that the authenticated user follows, with pagination support. Returns articles ordered by creation date with full metadata.<br/><b>`export const createArticle = async (article: any, id: number)`</b>: Creates a new article with validation for required fields (title, description, body), generates a unique slug, and associates tags and author. Throws exceptions for validation errors or duplicate titles.<br/><b>`export const getArticle = async (slug: string, id?: number)`</b>: Retrieves a single article by its slug identifier with full metadata including tags, author profile, and favorites. Throws a 404 exception if the article is not found.<br/><b>`export const updateArticle = async (article: any, slug: string, id: number)`</b>: Updates an existing article with authorization checks to ensure only the author can modify it. Handles slug regeneration if title changes, validates uniqueness, and updates tags. Throws exceptions for unauthorized access or validation errors.<br/><b>`export const deleteArticle = async (slug: string, id: number)`</b>: Deletes an article by slug with authorization verification to ensure only the author can delete it. Throws exceptions for non-existent articles or unauthorized access.<br/><b>`export const getCommentsByArticle = async (slug: string, id?: number)`</b>: Retrieves all comments for a specific article filtered by demo mode and optional user ID. Returns comments with author profiles and following status relative to the requesting user.<br/><b>`export const addComment = async (body: string, slug: string, id: number)`</b>: Creates a new comment on an article with validation for required body field. Associates the comment with the article and authenticated user, returning the comment with author profile.<br/><b>`export const deleteComment = async (id: number, userId: number)`</b>: Deletes a comment by ID with authorization checks to ensure only the comment author can delete it. Throws exceptions for non-existent comments or unauthorized access.<br/><b>`export const favoriteArticle = async (slugPayload: string, id: number)`</b>: Adds the authenticated user to the article's favorited list and returns the updated article with recalculated favorite count and user-specific favorite status.<br/><b>`export const unfavoriteArticle = async (slugPayload: string, id: number)`</b>: Removes the authenticated user from the article's favorited list and returns the updated article with recalculated favorite count and user-specific favorite status. |
| `src/app/routes/article/author.mapper.ts` | Provides a data transformation utility that maps raw author data objects into a standardized author profile format for the RealWorld application. Specifically handles the transformation of user information including username, bio, image, and following status based on the authenticated user's relationship with the author. | <b>`const authorMapper = (author: any, id?: number) => ({...})`</b>: Transforms a raw author object into a standardized author profile format, determining if the current user (identified by optional id parameter) is following the author by checking the followedBy array. |
| `src/app/routes/article/comment.model.ts` | Defines the TypeScript interface for a Comment entity in the RealWorld example application. This interface represents the data structure for user comments on articles, including metadata fields and an optional reference to the parent Article. |  |

## `src/app/routes/auth`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/app/routes/auth/auth.controller.ts` | Defines the Express router controller for authentication and user management endpoints in a RealWorld example application. Exposes REST API routes for user registration, login, retrieving current user information, and updating user profiles, delegating business logic to the auth service layer. | <b>`router.post('/users', async (req: Request, res: Response, next: NextFunction))`</b>: Creates a new user account with the provided user data and returns the created user with a 201 status code.<br/><b>`router.post('/users/login', async (req: Request, res: Response, next: NextFunction))`</b>: Authenticates a user with login credentials and returns the authenticated user information.<br/><b>`router.get('/user', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Retrieves the current authenticated user's information using the user ID from the authentication token.<br/><b>`router.put('/user', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Updates the current authenticated user's profile information and returns the updated user data. |
| `src/app/routes/auth/auth.service.ts` | Provides authentication and user management services for a RealWorld example application. Handles user registration with uniqueness validation, login with password verification, retrieval of current user data, and user profile updates. Integrates with Prisma ORM for database operations and uses bcrypt for password hashing and JWT token generation. | <b>`export const createUser = async (input: RegisterInput): Promise<RegisteredUser>`</b>: Registers a new user by validating input fields, checking email and username uniqueness, hashing the password, creating the user record in the database, and returning the user data with an authentication token.<br/><b>`export const login = async (userPayload: any)`</b>: Authenticates a user by validating credentials, comparing the provided password with the stored hashed password, and returning user data with a new authentication token if successful.<br/><b>`export const getCurrentUser = async (id: number)`</b>: Retrieves the current user's profile information from the database by user ID and returns it with a refreshed authentication token.<br/><b>`export const updateUser = async (userPayload: any, id: number)`</b>: Updates user profile information including email, username, password, image, and bio fields, hashing the new password if provided, and returns the updated user data with a new authentication token. |
| `src/app/routes/auth/auth.ts` | Provides JWT-based authentication middleware for Express routes in a RealWorld example application. Exports two authentication strategies: required (enforces JWT validation) and optional (allows requests without credentials), both configured to extract tokens from Authorization headers using Bearer or Token schemes. | <b>`const getTokenFromHeaders = (req: express.Request): string | null`</b>: Extracts and returns the JWT token from the Authorization header if it uses Bearer or Token scheme, otherwise returns null. |
| `src/app/routes/auth/register-input.model.ts` | Defines the TypeScript interface for user registration input data in the authentication module. Specifies the required and optional fields needed to register a new user account, including email, username, password, and optional profile information like image, bio, and demo flag. |  |
| `src/app/routes/auth/registered-user.model.ts` | Defines the TypeScript interface for a registered user entity in the authentication domain. This interface specifies the complete data structure returned after successful user registration or authentication, including user profile fields and authentication token. |  |
| `src/app/routes/auth/token.utils.ts` | Provides JWT token generation utility for user authentication in the RealWorld example application. Encapsulates the logic for creating signed JSON Web Tokens with user ID payload and configurable expiration time, using either environment-based or default secret keys. | <b>`const generateToken = (id: number): string`</b>: Generates and returns a signed JWT token containing the user ID, with a 60-day expiration period, using the JWT_SECRET environment variable or a default fallback secret. |
| `src/app/routes/auth/user-request.d.ts` | Provides TypeScript type augmentation for the Express Request object to include optional authentication metadata. Extends the Express namespace to add an auth property containing user identification information for authenticated requests. |  |
| `src/app/routes/auth/user.model.ts` | Defines the User data model interface for the RealWorld application authentication system. Represents the core user entity with profile information, social relationships (followers/following), content associations (articles, favorites, comments), and account metadata. |  |

## `src/app/routes/profile`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/app/routes/profile/profile.controller.ts` | Defines Express.js routing controller for user profile operations in a RealWorld application. Exposes three REST endpoints for retrieving user profiles, following users, and unfollowing users, delegating business logic to the profile service layer. | <b>`router.get('/profiles/:username', auth.optional, async (req: Request, res: Response, next: NextFunction) => {...})`</b>: Retrieves and returns a user profile by username with optional authentication, allowing both authenticated and anonymous access.<br/><b>`router.post('/profiles/:username/follow', auth.required, async (req: Request, res: Response, next: NextFunction) => {...})`</b>: Allows an authenticated user to follow another user by username and returns the updated profile.<br/><b>`router.delete('/profiles/:username/follow', auth.required, async (req: Request, res: Response, next: NextFunction) => {...})`</b>: Allows an authenticated user to unfollow another user by username and returns the updated profile. |
| `src/app/routes/profile/profile.model.ts` | Defines the TypeScript interface for a user Profile entity in the RealWorld application. This interface specifies the structure for profile data including username, bio, image URL, and following status, serving as a data contract for profile-related operations across the application. |  |
| `src/app/routes/profile/profile.service.ts` | Provides business logic services for user profile operations in a social networking application. Handles retrieving user profiles, following users, and unfollowing users through Prisma ORM database interactions. Maps database results to profile response objects and throws HTTP exceptions for error cases. | <b>`export const getProfile = async (usernamePayload: string, id?: number)`</b>: Retrieves a user profile by username from the database, including follower relationships, and returns a mapped profile object. Throws a 404 HTTP exception if the profile is not found.<br/><b>`export const followUser = async (usernamePayload: string, id: number)`</b>: Creates a follow relationship between the authenticated user (by id) and the target user (by username), then returns the updated profile with follower information.<br/><b>`export const unfollowUser = async (usernamePayload: string, id: number)`</b>: Removes the follow relationship between the authenticated user (by id) and the target user (by username), then returns the updated profile with follower information. |
| `src/app/routes/profile/profile.utils.ts` | Provides a utility function for transforming User domain entities into Profile view models for the profile feature. Maps user data including username, bio, image, and following status based on the authenticated user's relationship to the target user. | <b>`const profileMapper = (user: any, id: number | undefined): Profile`</b>: Transforms a User entity into a Profile object, determining the following status by checking if the authenticated user (identified by id) is in the target user's followedBy list. |

